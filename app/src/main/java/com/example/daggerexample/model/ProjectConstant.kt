package com.example.daggerexample.model

object ProjectConstant {
    var resourcesRegex = "[0-9]+ meters"
    var enemyRegex = "[a-z,A-Z]+ is Enemy"
    var healthPowerRegex = "has [0-9]+ hp"
    var attackPointsRegex = "attack is [0-9]+"
    var positionRegex = "[a-z,A-Z]+ at position [0-9]+"
}